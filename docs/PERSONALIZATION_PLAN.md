# 아이 진로 탐색 앱 - 개인화 강화 구현 계획

> 작성일: 2025-12-02
> 수정일: 2025-12-03
> 버전: 2.0 (IB 학습자상 통합)

**참조 문서**: `IB 학습자상 상세 분석 및 판별 가이드.pdf`

---

## 목차

1. [프로젝트 현황](#1-프로젝트-현황)
2. [개인화 강화 목표](#2-개인화-강화-목표)
3. [새로운 사용자 플로우](#3-새로운-사용자-플로우)
4. [데이터 구조 설계](#4-데이터-구조-설계)
5. [IB 학습자상 강화 설계](#5-ib-학습자상-강화-설계) *(신규)*
6. [통합 평가 프로세스](#6-통합-평가-프로세스) *(신규)*
7. [구현 Phase별 상세 계획](#7-구현-phase별-상세-계획)
8. [UI/UX 설계](#8-uiux-설계)
9. [API 설계](#9-api-설계)
10. [파일 구조 변경](#10-파일-구조-변경)
11. [구현 우선순위 및 일정](#11-구현-우선순위-및-일정)
12. [성공 지표](#12-성공-지표)

---

## 1. 프로젝트 현황

### 1.1 현재 구조

```
📁 child-future
├── app/
│   ├── page.tsx                    # 홈 (검사 모드 선택)
│   ├── basic-info/page.tsx         # 아이 기본정보 입력
│   ├── consultation/page.tsx       # 상담 피드백 입력
│   ├── assessment/page.tsx         # 성향 테스트
│   ├── results/page.tsx            # AI 분석 결과
│   └── api/
│       ├── generate-questions/     # 질문 개인화 API
│       └── analyze/                # 결과 분석 API
├── lib/
│   ├── types/                      # TypeScript 타입 정의
│   ├── data/                       # 정적 데이터 (질문, 직업군 등)
│   └── utils/                      # 유틸리티 함수
└── components/ui/                  # UI 컴포넌트
```

### 1.2 현재 개인화 방식

| 영역 | 현재 구현 | 한계점 |
|------|----------|--------|
| 질문 개인화 | 아이의 활동/취미/관심사로 질문 문구 변경 | 질문 구조는 동일, 깊이 탐색 없음 |
| AI 분석 | 입력 정보 기반 맞춤 인사이트 생성 | 부모 관점 완전 부재 |
| 결과 제공 | 직업군 + IB 학습자상 + 상담 가이드 | 부모-자녀 공동 활동 추천 없음 |
| 데이터 저장 | localStorage에 결과 저장 | 히스토리 비교 기능 미흡 |

### 1.3 현재 수집 데이터

**아이 정보 (BasicInfo)**
- 필수: 애칭, 나이
- 선택: 성별, 활동, 취미, 관심사, 잘하는 과목, 특별한 경험, 좋아하는 것, 꿈, 싫어하는 것

**상담 피드백 (ConsultationFeedback)**
- 학교 상담 내용
- 학원 상담 내용

---

## 2. 개인화 강화 목표

### 2.1 핵심 목표

1. **부모(엄마) 맞춤 가이드 제공** - 선택적 부모 프로필 수집
2. **부모-자녀 공동 활동 추천** - 함께 할 수 있는 구체적 활동
3. **아이 심층 이해** - 학습 스타일, 동기 요인 추가
4. **적응형 질문** - 응답에 따른 후속 질문
5. **성장 히스토리** - 이전 검사와 비교 기능

### 2.2 설계 원칙

- **선택적 참여**: 부모 정보는 100% 선택사항
- **점진적 공개**: 필수 → 선택 순서로 정보 요청
- **최소 부담**: 부모 설문은 5문항 이내
- **즉각적 가치**: 추가 정보 제공 시 더 풍부한 결과 제공

---

## 3. 새로운 사용자 플로우

### 3.1 메인 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                         [홈 페이지]                              │
│                    검사 모드 선택 (빠른/정밀)                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    [참여자 선택] (신규)                           │
│                                                                  │
│   ┌─────────────────┐         ┌─────────────────┐              │
│   │  👨‍👩‍👧 부모와 함께   │         │   👤 아이만      │              │
│   │  (부모 정보 입력)  │         │  (기존 플로우)    │              │
│   └────────┬────────┘         └────────┬────────┘              │
└────────────┼─────────────────────────────┼──────────────────────┘
             │                             │
             ▼                             │
┌────────────────────────┐                 │
│ [부모 프로필] (선택적)   │                 │
│ - 관계, 교육 목표       │                 │
│ - 가용 시간/자원        │                 │
│ - 현재 고민사항         │                 │
└───────────┬────────────┘                 │
            │                              │
            ▼                              │
┌────────────────────────┐                 │
│ [부모 간단 설문] (선택)  │                 │
│ - 양육 스타일 5문항     │                 │
│ - 건너뛰기 가능         │                 │
└───────────┬────────────┘                 │
            │                              │
            └──────────────┬───────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      [아이 기본정보]                              │
│              (확장: 학습 스타일, 동기 요인 추가)                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       [상담 내용]                                │
│                      (기존과 동일)                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       [성향 테스트]                              │
│              (확장: 적응형 후속 질문 추가)                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     [맞춤 결과 페이지]                            │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  아이 분석    │  │ 부모 가이드  │  │ 공동 활동    │          │
│  │  (기존+확장)  │  │ (부모 참여시) │  │  추천       │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 진행 단계 비교

**기존 (4단계)**
```
기본정보 → 상담내용 → 성향테스트 → 결과
```

**개선 (최대 6단계, 선택에 따라 4~6단계)**
```
[참여자 선택] → (부모정보) → (부모설문) → 아이정보 → 상담내용 → 성향테스트 → 결과

* 괄호(): 선택적 단계
```

---

## 4. 데이터 구조 설계

### 4.1 새로운 타입 정의

#### 부모 프로필 (ParentProfile)

```typescript
// lib/types/parent.ts

export type ParentRelation = 'mother' | 'father' | 'guardian' | 'grandparent' | 'teacher';
export type EducationPriority = 'academic' | 'character' | 'talent' | 'wellbeing' | 'balanced';
export type TimeAvailability = 'very-limited' | 'limited' | 'moderate' | 'abundant';
export type CommunicationStyle = 'directive' | 'supportive' | 'collaborative' | 'observant';

export interface ParentProfile {
  // 기본 정보
  nickname: string;
  relation: ParentRelation;

  // 교육 관점 (선택)
  educationPriority?: EducationPriority;
  educationGoals?: string[];           // 예: ["창의성 발달", "사회성 향상"]
  currentConcerns?: string[];          // 현재 고민 사항

  // 자원 (선택)
  availableTime?: TimeAvailability;
  weeklyHoursWithChild?: number;       // 주당 아이와 보내는 시간

  // 양육 스타일 점수 (설문 결과)
  parentingStyle?: {
    authoritative: number;   // 민주적 (따뜻함 + 규칙)
    authoritarian: number;   // 권위적 (엄격한 규칙)
    permissive: number;      // 허용적 (따뜻함, 규칙 없음)
    uninvolved: number;      // 방임적
  };

  // 의사소통 스타일 (설문 결과)
  communicationStyle?: CommunicationStyle;

  // 부모-자녀 상호작용
  currentActivities?: string[];        // 현재 함께 하는 활동
  preferredActivityTypes?: string[];   // 선호하는 활동 유형
}
```

#### 확장된 아이 정보 (EnhancedBasicInfo)

```typescript
// lib/types/assessment.ts 확장

export type LearningStyle = 'visual' | 'auditory' | 'kinesthetic' | 'reading';
export type SocialPreference = 'alone' | 'small-group' | 'large-group' | 'flexible';
export type EnergyPattern = 'morning' | 'afternoon' | 'evening' | 'variable';

export interface EnhancedBasicInfo extends BasicInfo {
  // 학습 스타일 (선택)
  learningPreference?: LearningStyle;

  // 사회적 성향 (선택)
  socialPreference?: SocialPreference;

  // 에너지 패턴 (선택)
  energyPattern?: EnergyPattern;

  // 동기 요인 (선택)
  motivators?: string[];      // 예: ["칭찬", "새로운 도전", "친구와 함께"]
  challenges?: string[];      // 예: ["집중 유지", "실패 두려움"]

  // 관심사 깊이 - 각 관심사별 1-5점 (선택)
  interestDepth?: Record<string, number>;
}
```

#### 부모-자녀 공동 활동 (ParentChildActivity)

```typescript
// lib/types/activity.ts

export interface ParentChildActivity {
  id: string;
  title: string;
  description: string;

  // 실행 정보
  duration: string;              // "30분", "1시간", "반나절"
  frequency: string;             // "매일", "주 2-3회", "주말"
  difficulty: 'easy' | 'medium' | 'challenging';
  cost: 'free' | 'low' | 'medium' | 'high';
  location: 'home' | 'outdoor' | 'facility' | 'anywhere';

  // 필요 자원
  materials?: string[];
  preparation?: string;

  // 매칭 조건
  suitableFor: {
    ageRange: [number, number];
    careerCategories: CareerCategory[];
    ibProfiles: IBProfile[];
    learningStyles?: LearningStyle[];
    parentTimeAvailability?: TimeAvailability[];
  };

  // 기대 효과
  benefits: {
    childDevelopment: string[];
    parentChildBond: string[];
    skillsBuilt: string[];
  };

  // 실행 가이드
  steps: string[];
  tips: string[];
  variations?: string[];         // 난이도/상황별 변형
}
```

#### 부모 맞춤 가이드 (ParentGuide)

```typescript
// lib/types/guide.ts

export interface ParentGuide {
  // 커뮤니케이션 가이드
  communication: {
    talkingPoints: string[];           // 아이와 나눌 대화 주제
    effectivePhrases: string[];        // 효과적인 표현
    avoidPhrases: string[];            // 피해야 할 표현
    encouragementTips: string[];       // 격려/칭찬 방법
  };

  // 진로 대화 가이드
  careerConversation: {
    howToIntroduce: string;            // 진로 대화 시작법
    questionsToAsk: string[];          // 아이에게 물어볼 질문
    howToRespond: string[];            // 아이 반응별 대응법
    followUpActivities: string[];      // 대화 후 활동
  };

  // 양육 스타일 맞춤 조언
  parentingAdvice: {
    strengths: string[];               // 현재 양육 스타일 강점
    improvements: string[];            // 개선 포인트
    actionItems: string[];             // 실천 항목
  };

  // 학습 지원 가이드
  learningSupport: {
    homeEnvironment: string[];         // 가정 환경 조성
    studyHabits: string[];             // 학습 습관 형성
    motivationTips: string[];          // 동기 부여 방법
  };
}
```

#### 통합 세션 데이터 (AssessmentSession)

```typescript
// lib/types/session.ts

export interface AssessmentSession {
  id: string;
  timestamp: string;
  mode: AssessmentMode;

  // 참여 유형
  participationType: 'child-only' | 'with-parent';

  // 데이터
  parentProfile?: ParentProfile;       // 부모 참여시에만
  basicInfo: EnhancedBasicInfo;
  consultation: ConsultationFeedback;
  responses: QuestionResponse[];

  // 결과
  result?: AssessmentResult;
}
```

### 4.2 확장된 결과 타입 (AssessmentResult)

```typescript
// lib/types/result.ts 확장

export interface EnhancedAssessmentResult extends AssessmentResult {
  // 기존 필드 유지
  // ...

  // 부모 참여시 추가 결과
  parentGuide?: ParentGuide;
  recommendedActivities?: ParentChildActivity[];

  // 성장 비교 (이전 검사 있을 경우)
  comparison?: {
    previousResultId: string;
    scoreChanges: Record<CareerCategory, number>;
    newStrengths: string[];
    growthAreas: string[];
    insights: string;
  };
}
```

---

## 5. IB 학습자상 강화 설계

> PDF "IB 학습자상 상세 분석 및 판별 가이드" 기반 개선

### 5.1 현재 vs 개선 비교

| 항목 | 현재 상태 | PDF 기준 개선 |
|------|----------|--------------|
| IB 프로필 정의 | ✅ 기본 정의 있음 | 이론적 심층 분석 추가 |
| 행동 지표 | ❌ 없음 | 상세 행동 지표 적용 |
| 관찰 척도 | ❌ 없음 | 숙련/능숙/초보 3단계 |
| 평가 질문 | ⚠️ ibWeights만 있음 | STAR 기반 질문 추가 |
| 비즈니스 역량 | ❌ 없음 | 미래 진로 연계 매핑 |
| 연령별 적응 | ⚠️ 제한적 | 5-7/8-10/11-14세 구분 |

### 5.2 확장된 IB 프로필 인터페이스

```typescript
// lib/types/ib-enhanced.ts

export interface BehavioralIndicator {
  id: string;
  category: 'cognitive' | 'emotional' | 'social' | 'behavioral';
  indicator: string;
  childVersion: string;  // 아동용 표현
  observableIn: ('home' | 'school' | 'play' | 'social')[];
}

export interface ObservationRubric {
  level: 'expert' | 'proficient' | 'novice';
  levelKo: '숙련' | '능숙' | '초보';
  description: string;
  childDescription: string;
  examples: string[];
}

export interface AssessmentQuestion {
  id: string;
  type: 'observation' | 'interview' | 'situation';
  question: string;
  childAdapted: string;
  followUp: string[];
  targetAge: { min: number; max: number };
  forObserver: 'parent' | 'teacher' | 'both';
}

export interface BusinessCompetency {
  competency: string;
  competencyKo: string;
  description: string;
  futureRelevance: string;  // 미래 진로 연관성
}

export interface EnhancedIBProfile {
  id: IBProfile;
  nameKo: string;
  nameEn: string;

  // 기존 필드 (유지)
  definition: string;
  characteristics: string[];
  educationMethods: {
    parentGuide: string[];
    teacherGuide: string[];
    activities: string[];
    avoidBehaviors: string[];
  };

  // 신규 필드 (PDF 기반)
  theoreticalBasis: {
    coreEssence: string;
    psychologicalFoundation: string;
    developmentalImportance: string;
  };

  behavioralIndicators: BehavioralIndicator[];
  observationRubrics: ObservationRubric[];
  assessmentQuestions: AssessmentQuestion[];
  businessCompetencies: BusinessCompetency[];

  // 아동 발달 단계별 표현
  ageAdaptations: {
    early: { age: '5-7', indicators: string[], activities: string[] };
    middle: { age: '8-10', indicators: string[], activities: string[] };
    late: { age: '11-14', indicators: string[], activities: string[] };
  };
}
```

### 5.3 IB 프로필-비즈니스 역량 매핑 (PDF 기반)

| IB 프로필 | 핵심 비즈니스 역량 | 미래 진로 연관 |
|----------|------------------|---------------|
| Inquirer | Learning Agility, R&D | 연구원, 과학자, 혁신가 |
| Knowledgeable | Subject Matter Expertise | 전문가, 컨설턴트, 교수 |
| Thinker | Strategic Thinking, Analysis | 전략가, 분석가, 기획자 |
| Communicator | Stakeholder Management | 마케터, PR, 협상가 |
| Principled | Ethics & Compliance | 법률가, 감사, 윤리담당 |
| Open-minded | Cross-cultural Competence | 외교관, 글로벌 리더 |
| Caring | Emotional Intelligence | 상담사, 의료인, 교육자 |
| Risk-taker | Entrepreneurship | 창업가, 투자자, 탐험가 |
| Balanced | Work-Life Integration | 웰니스, HR, 코치 |
| Reflective | Self-Development | 리더, 멘토, 작가 |

### 5.4 관찰 체크리스트 (부모/교사용)

```typescript
// lib/data/observation-checklist.ts

export interface ObservationItem {
  id: string;
  profileTarget: IBProfile[];
  question: string;
  frequency: 'never' | 'rarely' | 'sometimes' | 'often' | 'always';
  context: 'home' | 'school' | 'both';
  ageRange: { min: number; max: number };
}

export const OBSERVATION_CHECKLIST: ObservationItem[] = [
  // Inquirer 관련
  {
    id: 'OBS-INQ-01',
    profileTarget: ['Inquirer'],
    question: '새로운 것을 보면 "왜?", "어떻게?" 등의 질문을 자주 합니다',
    frequency: 'sometimes',
    context: 'both',
    ageRange: { min: 5, max: 14 }
  },
  // Thinker 관련
  {
    id: 'OBS-THK-01',
    profileTarget: ['Thinker'],
    question: '문제가 생기면 여러 방법을 생각해보고 해결하려 합니다',
    frequency: 'sometimes',
    context: 'both',
    ageRange: { min: 5, max: 14 }
  },
  // Caring 관련
  {
    id: 'OBS-CAR-01',
    profileTarget: ['Caring'],
    question: '친구나 가족이 힘들어하면 위로하거나 도와주려 합니다',
    frequency: 'sometimes',
    context: 'both',
    ageRange: { min: 5, max: 14 }
  },
  // Risk-taker 관련
  {
    id: 'OBS-RSK-01',
    profileTarget: ['Risk-taker'],
    question: '새로운 것을 시도하는 것을 두려워하지 않습니다',
    frequency: 'sometimes',
    context: 'both',
    ageRange: { min: 5, max: 14 }
  },
  // ... 총 20개 체크리스트 항목
];
```

### 5.5 연령별 상황 질문 예시

**5-7세 (Early)**
```typescript
{
  id: 'SIT-EARLY-01',
  ageGroup: 'early',
  scenario: '새 장난감을 받았어요. 뭘 제일 먼저 하고 싶어요?',
  visualSupport: '/images/situations/new-toy.png',
  options: [
    { text: '어떻게 움직이는지 뜯어보고 싶어요', ibWeights: { Inquirer: 2, Thinker: 1 } },
    { text: '친구들이랑 같이 가지고 놀고 싶어요', ibWeights: { Communicator: 2, Caring: 1 } },
    { text: '예쁘게 꾸며보고 싶어요', ibWeights: { 'Open-minded': 1, 'Risk-taker': 1 } },
    { text: '설명서대로 조립해보고 싶어요', ibWeights: { Knowledgeable: 1, Principled: 1 } }
  ]
}
```

**8-10세 (Middle)**
```typescript
{
  id: 'SIT-MIDDLE-01',
  ageGroup: 'middle',
  scenario: '학교에서 자유 주제로 발표를 하게 됐어요. 어떤 주제로 하고 싶어요?',
  options: [
    { text: '내가 직접 실험해본 과학 주제', ibWeights: { Inquirer: 2, 'Risk-taker': 1 } },
    { text: '친구들을 도울 수 있는 방법', ibWeights: { Caring: 2, Principled: 1 } },
    { text: '내가 그린 그림이나 만든 작품 소개', ibWeights: { 'Open-minded': 1, Communicator: 1 } },
    { text: '우리 반을 더 좋게 만드는 아이디어', ibWeights: { Thinker: 1, Principled: 1 } }
  ]
}
```

**11-14세 (Late)**
```typescript
{
  id: 'SIT-LATE-01',
  ageGroup: 'late',
  scenario: '동아리를 만들 수 있다면 어떤 동아리를 만들고 싶어요?',
  options: [
    { text: '과학 탐구 또는 발명 동아리', ibWeights: { Inquirer: 2, Thinker: 2 } },
    { text: '봉사 활동 또는 환경 보호 동아리', ibWeights: { Caring: 2, Principled: 2 } },
    { text: '예술, 음악, 창작 동아리', ibWeights: { 'Open-minded': 2, Communicator: 1 } },
    { text: '토론 또는 학생회 관련 동아리', ibWeights: { Communicator: 2, Thinker: 1 } }
  ]
}
```

### 5.6 진로-IB 심층 매핑

```typescript
// lib/data/career-ib-mapping.ts

export const CAREER_IB_DEEP_MAPPING = {
  creative: {
    primaryProfiles: ['Open-minded', 'Risk-taker', 'Communicator'],
    competencies: ['창의적 문제해결', '표현력', '혁신적 사고'],
    futureFields: ['디자인', '예술', '콘텐츠 창작', 'UX/UI', '마케팅']
  },
  analytical: {
    primaryProfiles: ['Inquirer', 'Thinker', 'Knowledgeable'],
    competencies: ['분석적 사고', '연구 역량', '논리적 추론'],
    futureFields: ['과학자', '연구원', '데이터 분석가', '엔지니어', '의사']
  },
  caring: {
    primaryProfiles: ['Caring', 'Communicator', 'Principled'],
    competencies: ['공감 능력', '대인관계', '윤리의식'],
    futureFields: ['교사', '상담사', '사회복지사', '의료인', 'NGO']
  },
  leadership: {
    primaryProfiles: ['Risk-taker', 'Communicator', 'Principled'],
    competencies: ['리더십', '의사결정', '비전 제시'],
    futureFields: ['경영자', '정치인', '기업가', '프로젝트 매니저']
  },
  practical: {
    primaryProfiles: ['Balanced', 'Knowledgeable', 'Risk-taker'],
    competencies: ['실행력', '기술 역량', '문제해결'],
    futureFields: ['엔지니어', '기술자', '건축가', '요리사', '장인']
  }
};
```

---

## 6. 통합 평가 프로세스

### 6.1 3단계 평가 프로세스 (PDF 기반)

```
┌─────────────────────────────────────────────────────────────┐
│                    통합 평가 프로세스                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1단계: 관찰 (Observation)                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • 부모/교사 관찰 체크리스트                            │   │
│  │ • 행동 지표 기반 빈도 체크                            │   │
│  │ • 일상 상황에서의 자연스러운 관찰                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  2단계: 탐색 (Inquiry) - 선택적                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • 상황 기반 질문 (아동용 적응)                         │   │
│  │ • 아이의 생각/선호도 직접 수집                         │   │
│  │ • 부모-아이 함께하는 활동 관찰                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  3단계: 검증 (Verification)                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • AI 분석 결과와 관찰 데이터 교차 검증                  │   │
│  │ • 이전 검사 결과와 비교                               │   │
│  │ • 발달 단계 고려한 해석                               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 평가 모드별 프로세스

| 모드 | 프로세스 | 소요 시간 | 대상 |
|------|---------|----------|------|
| **간소화** | 관찰 체크리스트(10) → AI 분석 → 결과 | 5-10분 | 빠른 스크리닝 |
| **표준** | 관찰 체크리스트 → 성향 검사(24) → AI 분석 → 결과 | 15-20분 | 일반 성향 파악 |
| **심화** | 관찰 → 성향 → 상황 질문 → 부모/아이 활동 → 심층 AI → 상세 결과 | 30-45분 | 심층 분석/상담 |

### 6.3 관찰 척도 (3단계 루브릭)

**예시: Inquirer (탐구하는 사람)**

| 수준 | 설명 | 아동 맥락 예시 |
|------|------|---------------|
| **숙련** | 복잡한 질문을 스스로 생성하고 체계적으로 탐구 | 프로젝트 주제를 스스로 정하고 계획을 세움 |
| **능숙** | 주어진 주제에 대해 깊이 있는 질문을 함 | 수업 내용에서 더 알고 싶은 것을 질문함 |
| **초보** | 기본적인 호기심을 보이나 탐구는 제한적 | 질문은 하지만 답을 기다림 |

### 6.4 결과 표시 강화

```typescript
// components/results/IBProfileResult.tsx

interface IBProfileResultProps {
  profiles: IBProfile[];
  scores: Record<IBProfile, number>;
  childAge: number;
}

// 표시 내용:
// 1. 상위 2-3개 IB 프로필 (레이더 차트)
// 2. 각 프로필별:
//    - 프로필 설명 (아이 연령에 맞춘 표현)
//    - 관찰된 행동 지표 (체크리스트 기반)
//    - 숙련도 수준 (3단계 루브릭)
//    - 연계 비즈니스 역량 (미래 진로 힌트)
// 3. 발달 단계별 활동 추천
// 4. 부모/교사 가이드
```

---

## 7. 구현 Phase별 상세 계획

### Phase 1: 데이터 구조 및 타입 정의

**목표**: 새로운 데이터 구조 정의 및 기존 코드와의 호환성 확보

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 1.1 | 부모 타입 생성 | `lib/types/parent.ts` | ParentProfile, 관련 타입 정의 |
| 1.2 | 활동 타입 생성 | `lib/types/activity.ts` | ParentChildActivity 타입 정의 |
| 1.3 | 가이드 타입 생성 | `lib/types/guide.ts` | ParentGuide 타입 정의 |
| 1.4 | 세션 타입 생성 | `lib/types/session.ts` | AssessmentSession 타입 정의 |
| 1.5 | 기존 타입 확장 | `lib/types/assessment.ts` | EnhancedBasicInfo 추가 |
| 1.6 | 결과 타입 확장 | `lib/types/result.ts` | EnhancedAssessmentResult 추가 |
| 1.7 | 타입 인덱스 | `lib/types/index.ts` | 모든 타입 re-export |

**예상 결과물**:
```
lib/types/
├── index.ts           # 통합 export
├── assessment.ts      # 확장됨
├── result.ts          # 확장됨
├── parent.ts          # 신규
├── activity.ts        # 신규
├── guide.ts           # 신규
└── session.ts         # 신규
```

---

### Phase 2: 정적 데이터 구축

**목표**: 부모 설문, 활동 데이터베이스, 가이드 템플릿 구축

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 2.1 | 부모 설문 문항 | `lib/data/parent-questions.ts` | 양육 스타일 파악용 5문항 |
| 2.2 | 활동 데이터베이스 | `lib/data/activities.ts` | 50+ 부모-자녀 활동 |
| 2.3 | 가이드 템플릿 | `lib/data/guide-templates.ts` | 조합별 가이드 템플릿 |
| 2.4 | 동기 요인 목록 | `lib/data/motivators.ts` | 아이 동기/도전 옵션 |
| 2.5 | 교육 목표 목록 | `lib/data/education-goals.ts` | 부모 교육 목표 옵션 |

**부모 설문 문항 예시**:

```typescript
// lib/data/parent-questions.ts

export const PARENT_QUESTIONS = [
  {
    id: 'pq1',
    text: '아이가 어려운 과제를 만났을 때, 주로 어떻게 하시나요?',
    options: [
      {
        label: 'A',
        text: '단계별로 나눠서 함께 해결해요',
        weights: { authoritative: 3, supportive: 2 }
      },
      {
        label: 'B',
        text: '스스로 해보도록 기다려요',
        weights: { permissive: 2, observant: 2 }
      },
      {
        label: 'C',
        text: '해야 하는 이유를 설명하고 격려해요',
        weights: { authoritative: 2, directive: 2 }
      },
      {
        label: 'D',
        text: '제가 먼저 시범을 보여줘요',
        weights: { directive: 3, authoritative: 1 }
      }
    ]
  },
  {
    id: 'pq2',
    text: '아이와 대화할 때 주로 어떤 방식을 사용하시나요?',
    options: [
      { label: 'A', text: '질문을 많이 하며 생각을 끌어내요', weights: { collaborative: 3 } },
      { label: 'B', text: '경험을 바탕으로 조언해줘요', weights: { directive: 3 } },
      { label: 'C', text: '아이 말을 끝까지 들어줘요', weights: { supportive: 3 } },
      { label: 'D', text: '아이가 먼저 말할 때까지 기다려요', weights: { observant: 3 } }
    ]
  },
  {
    id: 'pq3',
    text: '아이 교육에서 가장 중요하게 생각하는 것은?',
    options: [
      { label: 'A', text: '학업 성취와 실력', weights: { academic: 3 } },
      { label: 'B', text: '바른 인성과 가치관', weights: { character: 3 } },
      { label: 'C', text: '재능과 특기 개발', weights: { talent: 3 } },
      { label: 'D', text: '행복하고 건강한 성장', weights: { wellbeing: 3 } }
    ]
  },
  {
    id: 'pq4',
    text: '아이가 실수했을 때 어떻게 반응하시나요?',
    options: [
      { label: 'A', text: '왜 그랬는지 물어보고 함께 해결책을 찾아요', weights: { authoritative: 3 } },
      { label: 'B', text: '다음엔 어떻게 해야 하는지 알려줘요', weights: { directive: 3 } },
      { label: 'C', text: '괜찮다고 다독여주고 기다려요', weights: { supportive: 3 } },
      { label: 'D', text: '스스로 깨달을 때까지 지켜봐요', weights: { permissive: 2, observant: 2 } }
    ]
  },
  {
    id: 'pq5',
    text: '주말에 아이와 주로 어떻게 시간을 보내시나요?',
    options: [
      { label: 'A', text: '아이가 원하는 활동을 함께 해요', weights: { supportive: 2, collaborative: 2 } },
      { label: 'B', text: '교육적인 활동(학습, 체험)을 계획해요', weights: { directive: 2, academic: 2 } },
      { label: 'C', text: '각자 쉬면서 자유시간을 가져요', weights: { permissive: 3 } },
      { label: 'D', text: '함께 의논해서 활동을 정해요', weights: { authoritative: 2, collaborative: 2 } }
    ]
  }
];
```

**활동 데이터베이스 예시**:

```typescript
// lib/data/activities.ts

export const PARENT_CHILD_ACTIVITIES: ParentChildActivity[] = [
  {
    id: 'act-creative-1',
    title: '함께 그리는 상상 그림책',
    description: '아이와 번갈아가며 그림을 그리고 이야기를 만들어가는 활동',
    duration: '30-45분',
    frequency: '주 1-2회',
    difficulty: 'easy',
    cost: 'free',
    location: 'home',
    materials: ['스케치북', '색연필/크레파스'],
    suitableFor: {
      ageRange: [5, 12],
      careerCategories: ['creative', 'caring'],
      ibProfiles: ['Open-minded', 'Communicator', 'Risk-taker'],
      learningStyles: ['visual', 'kinesthetic'],
      parentTimeAvailability: ['limited', 'moderate', 'abundant']
    },
    benefits: {
      childDevelopment: ['창의력 발달', '스토리텔링 능력', '표현력 향상'],
      parentChildBond: ['공동 창작 경험', '서로의 상상력 이해'],
      skillsBuilt: ['협업', '아이디어 연결', '시각적 사고']
    },
    steps: [
      '빈 스케치북 첫 페이지에 부모가 간단한 그림 하나를 그립니다',
      '아이가 그 그림에 이어서 무언가를 추가합니다',
      '번갈아가며 그림을 추가하고, 각자 그린 것에 대해 이야기합니다',
      '완성된 그림을 보며 함께 이야기를 만들어봅니다'
    ],
    tips: [
      '정답이 없다는 것을 강조해주세요',
      '아이의 추가 그림에 대해 "왜 이걸 그렸어?"보다 "이건 뭘 하고 있는 거야?"로 물어보세요',
      '완성된 책에 제목을 붙이고 날짜를 적어두면 좋은 추억이 됩니다'
    ],
    variations: [
      '찰흙으로 함께 조형물 만들기',
      '레고로 이야기 장면 만들기'
    ]
  },
  {
    id: 'act-analytical-1',
    title: '주방 과학 실험',
    description: '집에 있는 재료로 간단한 과학 실험을 하며 원리를 탐구하는 활동',
    duration: '30-60분',
    frequency: '주 1회',
    difficulty: 'medium',
    cost: 'low',
    location: 'home',
    materials: ['베이킹소다', '식초', '식용색소', '컵', '접시'],
    suitableFor: {
      ageRange: [6, 14],
      careerCategories: ['analytical', 'practical'],
      ibProfiles: ['Inquirer', 'Thinker', 'Risk-taker'],
      learningStyles: ['kinesthetic', 'visual'],
      parentTimeAvailability: ['moderate', 'abundant']
    },
    benefits: {
      childDevelopment: ['과학적 사고력', '관찰력', '가설 검증 능력'],
      parentChildBond: ['함께 발견하는 즐거움', '호기심 공유'],
      skillsBuilt: ['실험 설계', '결과 분석', '안전 의식']
    },
    steps: [
      '오늘 실험 주제를 함께 정합니다 (예: 화산 폭발)',
      '어떤 일이 일어날지 아이에게 예측해보게 합니다',
      '함께 재료를 준비하고 안전 규칙을 확인합니다',
      '실험을 진행하며 관찰한 것을 이야기합니다',
      '왜 그런 결과가 나왔는지 함께 이야기합니다'
    ],
    tips: [
      '아이의 예측이 틀려도 "왜 그렇게 생각했어?"라고 물어보세요',
      '실험 결과를 사진으로 찍어 기록해두세요',
      '유튜브에서 관련 영상을 함께 찾아보는 것도 좋아요'
    ],
    variations: [
      '식물 성장 관찰 일지 (장기 프로젝트)',
      '날씨 관찰 및 예측 놀이'
    ]
  },
  // ... 더 많은 활동들
];
```

---

### Phase 3: 부모 정보 수집 UI

**목표**: 선택적 부모 프로필 및 설문 페이지 구현

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 3.1 | 참여자 선택 UI | `app/page.tsx` 수정 | 부모 참여 여부 선택 추가 |
| 3.2 | 부모 프로필 페이지 | `app/parent-info/page.tsx` | 부모 기본 정보 입력 |
| 3.3 | 부모 설문 페이지 | `app/parent-assessment/page.tsx` | 양육 스타일 설문 |
| 3.4 | 스텝퍼 업데이트 | `components/ui/stepper.tsx` | 동적 단계 표시 |
| 3.5 | 건너뛰기 로직 | 각 페이지 | 선택 단계 스킵 처리 |

**부모 프로필 페이지 UI 구조**:

```
┌─────────────────────────────────────────────────────────────────┐
│  [진행 단계 표시]                                                 │
│  참여자 → (부모정보) → (부모설문) → 아이정보 → 상담 → 검사 → 결과   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  부모님 정보 (선택)                                               │
│  더 맞춤화된 가이드를 받으실 수 있어요                             │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ 호칭 *                                                  │     │
│  │ [엄마 ▼]                                               │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ 아이 교육에서 가장 중요하게 생각하는 것 (복수 선택)       │     │
│  │ [ ] 학업 성취   [ ] 인성 발달   [ ] 재능 개발            │     │
│  │ [ ] 사회성     [ ] 행복/건강   [ ] 자기주도성            │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ 현재 아이 교육 관련 고민이 있다면?                       │     │
│  │ [자유 입력 텍스트 영역]                                 │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ 주당 아이와 함께 보내는 시간                            │     │
│  │ ○ 5시간 미만  ○ 5-10시간  ○ 10-20시간  ○ 20시간 이상   │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │  건너뛰기         │  │  다음 →          │                    │
│  └──────────────────┘  └──────────────────┘                    │
│                                                                  │
│  💡 이 단계를 건너뛰어도 기본 결과는 동일하게 제공됩니다          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

### Phase 4: 아이 정보 확장

**목표**: 기존 기본정보 페이지에 학습 스타일, 동기 요인 추가

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 4.1 | 학습 스타일 섹션 | `app/basic-info/page.tsx` | 선택적 학습 스타일 입력 |
| 4.2 | 동기 요인 섹션 | `app/basic-info/page.tsx` | 선택적 동기/도전 요인 |
| 4.3 | 에너지 패턴 | `app/basic-info/page.tsx` | 선택적 활동 시간대 |
| 4.4 | 관심사 깊이 | `app/basic-info/page.tsx` | 관심사별 깊이 슬라이더 |

**추가 UI 섹션**:

```
┌─────────────────────────────────────────────────────────────────┐
│  아이를 더 잘 이해하기 위한 추가 정보 (선택)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  아이가 가장 잘 배우는 방식은?                                    │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────┐│
│  │ 👁️ 보면서   │ │ 👂 들으면서 │ │ 🖐️ 해보면서 │ │ 📖 읽으면서 ││
│  │   배워요    │ │   배워요    │ │   배워요    │ │   배워요   ││
│  └─────────────┘ └─────────────┘ └─────────────┘ └────────────┘│
│                                                                  │
│  아이의 의욕을 높이는 것은? (복수 선택)                           │
│  [칭찬] [보상] [새로운 도전] [친구와 함께] [스스로 선택] [경쟁]   │
│                                                                  │
│  아이가 어려워하는 것은? (복수 선택)                              │
│  [오래 집중하기] [새로운 환경] [실패 경험] [혼자 하기] [발표]     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

### Phase 5: 적응형 질문 시스템

**목표**: 응답에 따라 후속 질문이 동적으로 추가되는 시스템

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 5.1 | 적응형 질문 정의 | `lib/data/adaptive-questions.ts` | 조건부 후속 질문 |
| 5.2 | 질문 엔진 수정 | `app/api/generate-questions/route.ts` | 적응형 로직 추가 |
| 5.3 | 평가 페이지 수정 | `app/assessment/page.tsx` | 동적 질문 처리 |
| 5.4 | 점수 계산 수정 | `lib/utils/scoring.ts` | 적응형 질문 가중치 |

**적응형 질문 예시**:

```typescript
// lib/data/adaptive-questions.ts

export const ADAPTIVE_QUESTIONS = [
  {
    triggerId: 'q1',  // "새로운 방법을 생각해내는 게 재미있어?"
    triggerCondition: { value: 5, operator: 'gte' },  // 5점(매우 그렇다) 이상
    followUpQuestion: {
      id: 'q1-follow',
      text: '새로운 방법을 생각해낼 때, 주로 어떻게 하는 편이야?',
      type: 'situation',
      options: [
        { label: 'A', text: '혼자 조용히 생각해요', weights: { creative: 2, analytical: 1 } },
        { label: 'B', text: '그림이나 낙서를 하면서 생각해요', weights: { creative: 3 } },
        { label: 'C', text: '친구나 가족과 이야기하면서요', weights: { creative: 1, caring: 2 } },
        { label: 'D', text: '일단 해보면서 생각해요', weights: { creative: 2, practical: 2 } }
      ]
    }
  },
  {
    triggerId: 'q9',  // "다른 사람의 기분을 잘 알아차리는 것 같아?"
    triggerCondition: { value: 4, operator: 'gte' },  // 4점 이상
    followUpQuestion: {
      id: 'q9-follow',
      text: '친구가 슬퍼보일 때, 주로 어떻게 해?',
      type: 'situation',
      options: [
        { label: 'A', text: '무슨 일인지 물어봐요', weights: { caring: 3, communicator: 2 } },
        { label: 'B', text: '옆에 조용히 있어줘요', weights: { caring: 2, reflective: 2 } },
        { label: 'C', text: '재미있는 걸 해서 기분 풀어줘요', weights: { caring: 2, creative: 1 } },
        { label: 'D', text: '선생님이나 어른한테 알려줘요', weights: { caring: 1, principled: 2 } }
      ]
    }
  }
];
```

---

### Phase 6: AI 분석 강화

**목표**: 부모 정보를 포함한 종합 분석 및 맞춤 가이드 생성

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 6.1 | 분석 프롬프트 확장 | `lib/utils/gemini.ts` | 부모 정보 포함 분석 |
| 6.2 | 활동 매칭 로직 | `lib/utils/activity-matcher.ts` | 조건 기반 활동 추천 |
| 6.3 | 가이드 생성 로직 | `lib/utils/guide-generator.ts` | 부모 맞춤 가이드 생성 |
| 6.4 | 통합 분석 API | `app/api/analyze/route.ts` | 통합 결과 반환 |

**확장된 Gemini 프롬프트 구조**:

```typescript
// 부모 정보 포함 시 추가되는 프롬프트 섹션

const parentContextPrompt = parentProfile ? `
## 부모 정보
- 관계: ${parentProfile.relation}
- 교육 우선순위: ${parentProfile.educationPriority}
- 교육 목표: ${parentProfile.educationGoals?.join(', ')}
- 현재 고민: ${parentProfile.currentConcerns?.join(', ')}
- 양육 스타일: ${JSON.stringify(parentProfile.parentingStyle)}
- 소통 스타일: ${parentProfile.communicationStyle}
- 가용 시간: ${parentProfile.availableTime}
- 현재 함께 하는 활동: ${parentProfile.currentActivities?.join(', ')}

## 부모 맞춤 가이드 요청
위 부모 정보를 고려하여 다음을 추가로 응답해주세요:

1. 이 부모의 양육 스타일 강점과 개선점
2. 아이의 성향에 맞는 커뮤니케이션 팁 3가지
3. 부모가 피해야 할 표현 2가지
4. 아이와 진로에 대해 대화하는 방법
5. 부모의 가용 시간과 아이 성향에 맞는 추천 활동 3가지
` : '';
```

---

### Phase 7: 결과 페이지 확장

**목표**: 부모 가이드, 공동 활동 추천 섹션 추가

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 7.1 | 결과 페이지 구조 변경 | `app/results/page.tsx` | 섹션 추가 |
| 7.2 | 부모 가이드 컴포넌트 | `components/ParentGuideSection.tsx` | 부모 맞춤 가이드 표시 |
| 7.3 | 활동 추천 컴포넌트 | `components/ActivityRecommendation.tsx` | 활동 카드 표시 |
| 7.4 | 대화 가이드 컴포넌트 | `components/ConversationGuide.tsx` | 진로 대화 가이드 |
| 7.5 | PDF 확장 | `app/results/page.tsx` | 부모 가이드 포함 PDF |

**결과 페이지 새 섹션 구조**:

```
[기존 섹션들]
- AI 분석
- 아이 프로필 요약
- 직업군별 점수
- 상위 직업군 카드
- 발전 팁
- IB 학습자상 가이드

[새 섹션 - 부모 참여시에만 표시]
┌─────────────────────────────────────────────────────────────────┐
│  👨‍👩‍👧 부모님을 위한 맞춤 가이드                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  💬 [닉네임]와 대화할 때                                          │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ ✅ 효과적인 표현                                         │    │
│  │ • "네 생각은 어때?"                                      │    │
│  │ • "천천히 해도 괜찮아"                                   │    │
│  │                                                          │    │
│  │ ❌ 피하면 좋은 표현                                      │    │
│  │ • "빨리빨리"                                             │    │
│  │ • "왜 그것밖에 못해?"                                    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  🎯 진로 대화 가이드                                             │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 시작하기: "요즘 가장 재미있는 건 뭐야?"                   │    │
│  │                                                          │    │
│  │ 물어볼 질문:                                             │    │
│  │ • "그걸 할 때 어떤 기분이 들어?"                         │    │
│  │ • "그걸 하는 사람들은 어떤 사람들일까?"                  │    │
│  │                                                          │    │
│  │ 아이가 "몰라요"라고 하면:                                │    │
│  │ • "괜찮아, 함께 알아보자"                                │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  🎨 함께 하면 좋은 활동                                          │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ 🎨          │ │ 🔬          │ │ 🏃          │               │
│  │ 함께 그리는 │ │ 주방 과학   │ │ 동네 탐험   │               │
│  │ 상상 그림책 │ │ 실험        │ │ 산책        │               │
│  │             │ │             │ │             │               │
│  │ 30분 | 집   │ │ 1시간 | 집  │ │ 1시간 | 야외│               │
│  │ [자세히 보기]│ │ [자세히 보기]│ │ [자세히 보기]│               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

### Phase 8: 히스토리 및 비교 기능

**목표**: 이전 검사와 비교, 성장 추적 기능

**작업 목록**:

| # | 작업 | 파일 | 설명 |
|---|------|------|------|
| 8.1 | 히스토리 페이지 | `app/history/page.tsx` | 과거 검사 목록 |
| 8.2 | 비교 기능 | `app/compare/[id]/page.tsx` | 두 검사 비교 |
| 8.3 | 성장 차트 | `components/GrowthChart.tsx` | 시간별 변화 차트 |
| 8.4 | 스토리지 확장 | `lib/utils/storage.ts` | 비교 관련 함수 |

---

## 8. UI/UX 설계

### 8.1 선택적 단계 표시

부모 정보 단계가 선택적임을 명확히 표시:

```
○ ← → ○ ← → ○ ← → ● ← → ○ ← → ○ ← → ○
참여자   부모    부모    아이    상담    검사    결과
선택    정보    설문    정보
       (선택)  (선택)
```

### 8.2 건너뛰기 UI 패턴

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  [부모 정보 입력 폼]                                             │
│                                                                  │
│  ───────────────────────────────────────────────────────────    │
│                                                                  │
│  ┌──────────────────────┐    ┌──────────────────────┐          │
│  │   이 단계 건너뛰기    │    │     다음 →           │          │
│  │   (회색, outline)    │    │    (파란색, solid)   │          │
│  └──────────────────────┘    └──────────────────────┘          │
│                                                                  │
│  💡 건너뛰어도 기본 결과는 동일합니다.                            │
│     부모 정보를 입력하시면 맞춤 양육 가이드를 추가로 받으실 수 있어요. │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.3 결과 페이지 탭 구조 (부모 참여시)

```
┌─────────────────────────────────────────────────────────────────┐
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 아이 분석 │ │ 부모 가이드│ │ 함께 활동 │ │ 성장 비교 │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│      ▲                                                          │
│      │ 활성 탭                                                   │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                                  │
│  [선택된 탭의 내용]                                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. API 설계

### 9.1 새로운 API 엔드포인트

| 엔드포인트 | 메서드 | 설명 |
|-----------|--------|------|
| `/api/generate-questions` | POST | 기존 + 적응형 질문 추가 |
| `/api/analyze` | POST | 기존 + 부모 가이드 추가 |
| `/api/match-activities` | POST | 활동 매칭 (신규) |
| `/api/generate-guide` | POST | 부모 가이드 생성 (신규) |
| `/api/compare` | POST | 검사 비교 (신규) |

### 9.2 API 요청/응답 구조

**활동 매칭 API**:

```typescript
// POST /api/match-activities
// Request
{
  childProfile: {
    age: number;
    topCategories: CareerCategory[];
    ibProfiles: IBProfile[];
    learningStyle?: LearningStyle;
  };
  parentProfile?: {
    availableTime: TimeAvailability;
    preferredActivityTypes?: string[];
  };
  limit?: number;  // 기본값: 5
}

// Response
{
  activities: ParentChildActivity[];
  matchScores: Record<string, number>;  // 활동 ID별 매칭 점수
}
```

**부모 가이드 생성 API**:

```typescript
// POST /api/generate-guide
// Request
{
  childResult: AssessmentResult;
  parentProfile: ParentProfile;
}

// Response
{
  guide: ParentGuide;
  recommendations: string[];
}
```

---

## 10. 파일 구조 변경

### 10.1 최종 파일 구조

```
📁 child-future
├── app/
│   ├── page.tsx                         # 홈 (참여자 선택 추가)
│   ├── parent-info/
│   │   └── page.tsx                     # 부모 프로필 입력 (신규)
│   ├── parent-assessment/
│   │   └── page.tsx                     # 부모 설문 (신규)
│   ├── basic-info/
│   │   └── page.tsx                     # 아이 정보 (확장)
│   ├── consultation/
│   │   └── page.tsx                     # 상담 내용 (기존)
│   ├── assessment/
│   │   └── page.tsx                     # 성향 테스트 (적응형 추가)
│   ├── results/
│   │   └── page.tsx                     # 결과 (섹션 추가)
│   ├── history/
│   │   └── page.tsx                     # 히스토리 (신규)
│   ├── compare/
│   │   └── [id]/page.tsx                # 비교 (신규)
│   └── api/
│       ├── generate-questions/route.ts  # 질문 생성 (확장)
│       ├── analyze/route.ts             # 분석 (확장)
│       ├── match-activities/route.ts    # 활동 매칭 (신규)
│       ├── generate-guide/route.ts      # 가이드 생성 (신규)
│       └── compare/route.ts             # 비교 (신규)
│
├── components/
│   ├── ui/                              # 기존 UI 컴포넌트
│   ├── ParentGuideSection.tsx           # 부모 가이드 (신규)
│   ├── ActivityRecommendation.tsx       # 활동 추천 (신규)
│   ├── ConversationGuide.tsx            # 대화 가이드 (신규)
│   ├── GrowthChart.tsx                  # 성장 차트 (신규)
│   ├── SkipButton.tsx                   # 건너뛰기 버튼 (신규)
│   ├── ObservationChecklist.tsx         # 관찰 체크리스트 (신규 IB)
│   ├── IBProfileResult.tsx              # IB 프로필 결과 (신규 IB)
│   ├── BusinessCompetencyCard.tsx       # 비즈니스 역량 카드 (신규 IB)
│   └── AgeAdaptedContent.tsx            # 연령별 콘텐츠 (신규 IB)
│
├── lib/
│   ├── types/
│   │   ├── index.ts                     # 통합 export
│   │   ├── assessment.ts                # 확장
│   │   ├── result.ts                    # 확장
│   │   ├── parent.ts                    # 신규
│   │   ├── activity.ts                  # 신규
│   │   ├── guide.ts                     # 신규
│   │   ├── session.ts                   # 신규
│   │   └── ib-enhanced.ts               # 신규 (IB 확장)
│   │
│   ├── data/
│   │   ├── questions.ts                 # 기존
│   │   ├── careers.ts                   # 기존
│   │   ├── majors.ts                    # 기존
│   │   ├── ib-profiles.ts               # 기존 (확장 예정)
│   │   ├── consultation.ts              # 기존
│   │   ├── theories.ts                  # 기존
│   │   ├── parent-questions.ts          # 신규
│   │   ├── activities.ts                # 신규
│   │   ├── guide-templates.ts           # 신규
│   │   ├── motivators.ts                # 신규
│   │   ├── education-goals.ts           # 신규
│   │   ├── adaptive-questions.ts        # 신규
│   │   ├── ib-enhanced-profiles.ts      # 신규 (IB 확장 데이터)
│   │   ├── observation-checklist.ts     # 신규 (관찰 체크리스트)
│   │   ├── situation-questions.ts       # 신규 (연령별 상황 질문)
│   │   └── career-ib-mapping.ts         # 신규 (진로-IB 매핑)
│   │
│   └── utils/
│       ├── gemini.ts                    # 확장
│       ├── scoring.ts                   # 확장
│       ├── storage.ts                   # 확장
│       ├── activity-matcher.ts          # 신규
│       ├── guide-generator.ts           # 신규
│       └── comparison.ts                # 신규
│
└── docs/
    └── PERSONALIZATION_PLAN.md          # 이 문서
```

---

## 11. 구현 우선순위 및 일정

### 우선순위 매트릭스

| Phase | 기능 | 영향력 | 난이도 | 우선순위 |
|-------|------|--------|--------|---------|
| 1 | 데이터 구조 정의 (IB 포함) | 기반 | 낮음 | **1** |
| 1-IB | IB 확장 타입 정의 | 기반 | 낮음 | **1** |
| 2 | 정적 데이터 구축 | 높음 | 중간 | **2** |
| 2-IB | IB 행동 지표/관찰 척도 | 높음 | 중간 | **2** |
| 3 | 부모 정보 수집 UI | 높음 | 낮음 | **3** |
| 3-IB | 관찰 체크리스트 UI | 높음 | 낮음 | **3** |
| 4 | 아이 정보 확장 | 중간 | 낮음 | **4** |
| 5 | 적응형 질문 | 중간 | 높음 | **5** |
| 5-IB | 연령별 상황 질문 | 중간 | 중간 | **5** |
| 6 | AI 분석 강화 (IB 통합) | 높음 | 중간 | **6** |
| 7 | 결과 페이지 확장 (IB 표시) | 높음 | 중간 | **7** |
| 8 | 히스토리/비교 | 중간 | 중간 | **8** |

### 권장 구현 순서 (IB 통합)

```
Sprint 1: Phase 1 + 1-IB + 2 + 2-IB (기반 구축)
   └→ 타입 정의 + IB 확장 타입 + 데이터 준비 + IB 행동 지표

Sprint 2: Phase 3 + 3-IB (핵심 UI)
   └→ 부모 UI + 관찰 체크리스트

Sprint 3: Phase 6 (AI 강화 - IB 통합)
   └→ AI 프롬프트에 IB 이론적 배경, 행동 지표 포함

Sprint 4: Phase 7 (결과 표시 - IB 강화)
   └→ IB 프로필 상세, 비즈니스 역량, 발달 단계별 활동

Sprint 5: Phase 4 + 5 + 5-IB (심화)
   └→ 아이 정보 + 적응형 질문 + 연령별 상황 질문

Sprint 6: Phase 8 (부가 기능)
   └→ 히스토리/비교
```

---

## 12. 성공 지표

### 12.1 정량적 지표

- 부모 프로필 입력률: 목표 40%+
- 부모 설문 완료율: 목표 70%+ (입력 시작한 경우)
- 결과 페이지 체류 시간: 목표 +30%
- PDF 다운로드율: 목표 +20%
- 재방문율: 목표 +25%

### 12.2 IB 관련 지표

- 관찰 체크리스트 완료율: 목표 60%+
- IB 프로필 결과 확인율: 목표 80%+
- 발달 단계별 활동 추천 클릭률: 목표 50%+
- 비즈니스 역량 정보 조회율: 목표 40%+

### 12.3 정성적 지표

- 부모 만족도 설문
- 활동 추천 유용성 평가
- 가이드 실천 여부 피드백
- IB 프로필 설명 이해도 평가

---

## 부록: 참고 자료

### A. 양육 스타일 이론

Diana Baumrind의 양육 스타일 분류:
- **권위적(Authoritative)**: 높은 반응성 + 높은 요구
- **권위주의적(Authoritarian)**: 낮은 반응성 + 높은 요구
- **허용적(Permissive)**: 높은 반응성 + 낮은 요구
- **방임적(Uninvolved)**: 낮은 반응성 + 낮은 요구

### B. VARK 학습 스타일 모델

- **Visual**: 시각적 학습자 - 그림, 도표, 영상
- **Auditory**: 청각적 학습자 - 강의, 토론, 설명
- **Reading/Writing**: 읽기/쓰기 학습자 - 텍스트, 노트
- **Kinesthetic**: 체험적 학습자 - 실습, 활동, 경험

### C. IB 학습자상 PDF 문서 (핵심 참조)

**문서명**: `IB 학습자상 상세 분석 및 판별 가이드.pdf`

**주요 내용**:
- 10가지 IB 학습자상 이론적 심층 분석
- 각 프로필별 행동 지표 (Behavioral Indicators)
- 3단계 관찰 척도 (숙련/능숙/초보)
- STAR 기반 평가 질문
- 비즈니스/리더십 역량 매핑
- 통합 평가 프로세스 (관찰 → 탐색 → 검증)

**적용 영역**:
- 섹션 5: IB 학습자상 강화 설계
- 섹션 6: 통합 평가 프로세스
- Phase 1-IB ~ 5-IB: IB 관련 구현 작업

### D. 참고 문헌

1. Baumrind, D. (1991). The influence of parenting style on adolescent competence and substance use.
2. Fleming, N. D., & Mills, C. (1992). Not Another Inventory, Rather a Catalyst for Reflection.
3. IBO (2017). IB Learner Profile.
4. Holland, J. L. (1997). Making Vocational Choices: A Theory of Vocational Personalities and Work Environments.

---

*문서 끝 - v2.0 (IB 학습자상 통합)*
