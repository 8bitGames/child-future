# 성장 관리 시스템 기능 정의서

> **문서 버전:** 1.1
> **작성일:** 2024-12-23
> **최종 수정:** 2024-12-23
> **목적:** 검사 결과를 시작점으로 부모와 아이가 함께 성장하는 여정을 관리하는 시스템

### 관련 문서
- 📚 [기술 인프라 설계서](./TECHNICAL_INFRASTRUCTURE.md) - Supabase 스키마, Vercel 배포 설정

---

## 1. 개요

### 1.1 현재 상태 분석

```
[현재 플로우]
메인 → 기본정보 → 상담내용 → 성향검사 → 결과 → (끝)
```

**문제점:**
- 일회성 진단으로 끝남
- 결과 활용 방안 부재
- 부모-아이 지속적 상호작용 채널 없음
- IB 학습자상 가이드의 실천/추적 시스템 없음

### 1.2 확장 목표

```
[확장된 플로우]
검사 → 결과 → 목표설정 → 활동미션 → 기록 → 성장확인 → 재검사
              ↑                              ↓
              ←←←←← 순환 사이클 ←←←←←←←←←←←←←
```

**핵심 철학:**
> "진단에서 끝나지 않고, 진단을 시작점으로 삼아 부모와 아이가 함께 성장하는 여정을 만든다"

---

## 2. 역할 정의

### 2.1 아이(Child) 영역

| 활동 | 설명 | 교육적 효과 |
|------|------|------------|
| 검사 참여 | 성향 검사 직접 응답 | 자기 인식 |
| 미션 선택 | 주간 활동 미션 스스로 선택 | 자율성, 주도성 |
| 활동 기록 | "오늘 한 일" 간단 기록 | 성찰 습관 |
| 목표 설정 | "이번 달 해보고 싶은 것" | 계획력 |
| 자기 평가 | 활동 후 별점/이모지로 만족도 표시 | 메타인지 |
| 포트폴리오 꾸미기 | 좋아하는 활동 사진 선택 | 자기 정체성 |

### 2.2 부모(Parent) 영역

| 활동 | 설명 | 왜 필요한가 |
|------|------|------------|
| 관찰 기록 | 아이의 일상 변화 메모 | 아이가 인지하지 못하는 성장 포착 |
| 환경 제공 | IB 학습자상에 맞는 경험 기회 | 활동/자원 접근성 확보 |
| 대화 촉진 | 상담 가이드 활용한 대화 | 아이의 생각/감정 표현 유도 |
| 격려와 피드백 | 구체적 칭찬 + 방향 제시 | 동기부여 |
| 리마인더 | 주간 미션 상기시키기 | 지속성 유지 |
| 리포트 검토 | 성장 리포트 함께 보기 | 성과 공유 및 다음 단계 논의 |
| 선생님 연계 | 학교/학원 상담 시 결과 활용 | 일관된 교육 방향 |

---

## 3. Phase 1: 성장 관리 기본 기능

### 3.1 결과 히스토리 페이지 (`/history`)

#### 3.1.1 기능 설명
과거 검사 결과를 시간순으로 보여주고, 결과 간 비교를 통해 성장 추이를 확인

#### 3.1.2 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  📊 검사 히스토리                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [성장 그래프 영역]                                      │
│  ┌─────────────────────────────────────────────────┐   │
│  │  직업군별 점수 추이 (라인 차트)                    │   │
│  │  - 창의·예술 ━━━━                               │   │
│  │  - 분석·연구 ━━━━                               │   │
│  │  - 사람·돌봄 ━━━━                               │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [검사 기록 리스트]                                      │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📅 2024.12.23 정밀검사                           │   │
│  │    상위: 창의·예술(85) > 분석·연구(72)            │   │
│  │    IB: 탐구하는, 생각하는                         │   │
│  │    [상세보기] [비교하기]                          │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ 📅 2024.09.15 빠른검사                           │   │
│  │    상위: 분석·연구(68) > 창의·예술(65)            │   │
│  │    [상세보기] [비교하기]                          │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 3.1.3 데이터 요구사항
- 기존 `localStorage` 저장 구조 활용
- `AssessmentResult[]` 시간순 정렬
- 비교 기능용 diff 계산 유틸리티

#### 3.1.4 상세 기능

| 기능 | 설명 | 우선순위 |
|------|------|---------|
| 결과 목록 | 시간순 검사 결과 카드 리스트 | P0 |
| 성장 그래프 | 직업군 점수 추이 라인 차트 | P0 |
| 결과 비교 | 두 검사 결과 나란히 비교 | P1 |
| IB 변화 추적 | IB 학습자상 변화 타임라인 | P1 |
| 삭제 기능 | 특정 결과 삭제 | P2 |

---

### 3.2 성장 목표 설정 페이지 (`/goals`)

#### 3.2.1 기능 설명
검사 결과 기반으로 AI가 3개월 성장 목표를 추천하고, 부모와 아이가 함께 목표 선택

#### 3.2.2 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  🎯 성장 목표 설정                                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [아이 프로필 요약]                                      │
│  민준이 (10세) | 탐구하는 사람, 생각하는 사람             │
│  상위 직업군: 창의·예술, 분석·연구                       │
│                                                         │
│  ────────────────────────────────────────────────       │
│                                                         │
│  [AI 추천 목표] 💡                                      │
│                                                         │
│  ┌───────────────────────────────────────────────┐     │
│  │ 🔍 탐구력 강화 목표                              │     │
│  │ "매주 1가지 궁금한 주제를 정해서 탐구해보기"      │     │
│  │                                                 │     │
│  │ 추천 활동:                                       │     │
│  │ • 도서관에서 관심 주제 책 찾기                   │     │
│  │ • 실험 관찰 일지 작성하기                        │     │
│  │ • "왜?"라고 3번 이상 질문하기                    │     │
│  │                                                 │     │
│  │ [이 목표 선택하기]                               │     │
│  └───────────────────────────────────────────────┘     │
│                                                         │
│  ┌───────────────────────────────────────────────┐     │
│  │ 💭 창의적 사고력 강화 목표                        │     │
│  │ "새로운 방식으로 문제 해결해보기"                 │     │
│  │ ...                                             │     │
│  └───────────────────────────────────────────────┘     │
│                                                         │
│  [+ 직접 목표 추가하기]                                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 3.2.3 목표 데이터 구조

```typescript
interface GrowthGoal {
  id: string;
  childId: string;              // 아이 식별자 (nickname 기반)
  title: string;                // 목표 제목
  description: string;          // 목표 설명
  targetIBProfile: IBProfile;   // 관련 IB 학습자상
  targetCategory: CareerCategory; // 관련 직업군
  period: {
    start: string;              // 시작일 (ISO)
    end: string;                // 종료일 (ISO)
  };
  recommendedActivities: string[]; // 추천 활동 목록
  status: 'active' | 'completed' | 'abandoned';
  progress: number;             // 0-100 진행률
  createdAt: string;
  updatedAt: string;
}
```

#### 3.2.4 AI 목표 추천 로직

```typescript
// Gemini API 프롬프트 구조
const goalRecommendationPrompt = `
아이 정보:
- 이름: ${nickname}
- 나이: ${age}세
- 상위 직업군: ${topCategories.join(', ')}
- IB 학습자상: ${ibProfiles.join(', ')}
- 관심사: ${interests.join(', ')}
- 활동: ${activities.join(', ')}

위 정보를 바탕으로 3개월간 달성 가능한 성장 목표 3가지를 추천해주세요.
각 목표에는 다음을 포함해주세요:
1. 목표 제목 (아이가 이해할 수 있는 쉬운 말로)
2. 목표 설명
3. 관련 IB 학습자상
4. 구체적인 추천 활동 3-5개
5. 성공 기준

JSON 형식으로 응답해주세요.
`;
```

#### 3.2.5 상세 기능

| 기능 | 설명 | 우선순위 |
|------|------|---------|
| AI 목표 추천 | 검사 결과 기반 3개 목표 추천 | P0 |
| 목표 선택 | 추천 목표 중 선택 | P0 |
| 직접 추가 | 사용자 정의 목표 추가 | P1 |
| 목표 수정 | 선택한 목표 내용 수정 | P1 |
| 진행률 표시 | 활동 완료 기반 자동 계산 | P1 |
| 목표 기간 설정 | 1/2/3개월 선택 | P2 |

---

### 3.3 주간 체크인 페이지 (`/check-in`)

#### 3.3.1 기능 설명
매주 부모와 아이가 함께 한 주를 돌아보고 기록하는 페이지

#### 3.3.2 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  📝 이번 주 체크인                     2024.12.16-12.22 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [아이 자기평가] 👶                                      │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 이번 주 나는...                                  │   │
│  │                                                 │   │
│  │ 1. 목표 활동을 했나요?                           │   │
│  │    😊 많이 했어요                                │   │
│  │    🙂 조금 했어요                                │   │
│  │    😐 별로 못했어요                              │   │
│  │                                                 │   │
│  │ 2. 가장 재미있었던 건?                           │   │
│  │    [________________________]                   │   │
│  │                                                 │   │
│  │ 3. 이번 주 기분 이모지                           │   │
│  │    😄 😊 🙂 😐 😢                               │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [부모 관찰기록] 👪                                      │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 이번 주 아이에게서 발견한 것                      │   │
│  │                                                 │   │
│  │ 1. 눈에 띄는 변화나 성장                         │   │
│  │    [________________________]                   │   │
│  │                                                 │   │
│  │ 2. 완료한 활동                                   │   │
│  │    ☑️ 도서관에서 공룡 책 읽기                    │   │
│  │    ☐ 실험 관찰 일지 작성                        │   │
│  │    ☑️ 자연 탐사 산책                            │   │
│  │                                                 │   │
│  │ 3. 특별한 에피소드 (선택)                        │   │
│  │    [________________________]                   │   │
│  │    [📷 사진 추가]                               │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [저장하기]                                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 3.3.3 체크인 데이터 구조

```typescript
interface WeeklyCheckIn {
  id: string;
  childId: string;
  weekStart: string;            // 주 시작일 (월요일)
  weekEnd: string;              // 주 종료일 (일요일)

  // 아이 자기평가
  childEvaluation: {
    activityLevel: 'high' | 'medium' | 'low';
    favoriteActivity: string;
    weeklyMood: '😄' | '😊' | '🙂' | '😐' | '😢';
    freeNote?: string;
  };

  // 부모 관찰기록
  parentObservation: {
    noticeableChanges: string;
    completedActivities: string[];
    specialEpisode?: string;
    photos?: string[];          // base64 또는 URL
  };

  // 메타데이터
  createdAt: string;
  updatedAt: string;
}
```

#### 3.3.4 상세 기능

| 기능 | 설명 | 우선순위 |
|------|------|---------|
| 아이 자기평가 | 간단한 이모지/선택형 평가 | P0 |
| 부모 관찰기록 | 텍스트 입력 + 활동 체크 | P0 |
| 사진 첨부 | 활동 사진 업로드 (최대 3장) | P1 |
| 주간 리마인더 | 일요일 저녁 알림 | P2 |
| 과거 기록 조회 | 이전 주 체크인 기록 보기 | P1 |

---

## 4. Phase 2: 상호작용 강화 기능

### 4.1 활동 미션 시스템 (`/missions`)

#### 4.1.1 기능 설명
IB 학습자상별 맞춤 미션을 제공하고, 완료 시 배지를 획득하는 게이미피케이션 시스템

#### 4.1.2 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  🎯 이번 주 미션                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [진행 중인 미션]                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 🔍 탐구하는 사람 미션                             │   │
│  │                                                 │   │
│  │ "도서관에서 궁금한 주제 책 1권 찾아보기"          │   │
│  │                                                 │   │
│  │ 난이도: ⭐                                       │   │
│  │ 보상: 🏅 탐구 배지 (브론즈)                       │   │
│  │                                                 │   │
│  │ [완료했어요!]  [다음에 할래요]                    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [추천 미션 더보기]                                      │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 💭 생각하는 사람 미션                             │   │
│  │ "오늘 있었던 일 중 왜 그랬을까? 생각해보기"       │   │
│  │ 난이도: ⭐  |  [시작하기]                         │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ 🚀 도전하는 사람 미션                             │   │
│  │ "처음 해보는 활동 1가지 도전하기"                 │   │
│  │ 난이도: ⭐⭐  |  [시작하기]                       │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [내 배지 컬렉션] 🏆                                    │
│  🏅🏅🏅⬜⬜ (3/10)                                     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 4.1.3 미션 데이터 구조

```typescript
interface Mission {
  id: string;
  title: string;
  description: string;
  targetIBProfile: IBProfile;
  difficulty: 1 | 2 | 3;        // 1: 쉬움, 2: 보통, 3: 어려움
  estimatedTime: string;        // "10분", "30분", "1시간"
  badge: {
    name: string;
    icon: string;
    tier: 'bronze' | 'silver' | 'gold';
  };
  ageRange: {
    min: number;
    max: number;
  };
  category: CareerCategory;
}

interface MissionProgress {
  id: string;
  childId: string;
  missionId: string;
  status: 'in_progress' | 'completed' | 'skipped';
  startedAt: string;
  completedAt?: string;
  reflection?: string;          // 완료 후 한 줄 소감
  parentVerified: boolean;      // 부모 확인 여부
}

interface Badge {
  id: string;
  childId: string;
  missionId: string;
  name: string;
  icon: string;
  tier: 'bronze' | 'silver' | 'gold';
  earnedAt: string;
}
```

#### 4.1.4 IB 학습자상별 미션 예시

```typescript
const MISSION_DATABASE: Mission[] = [
  // 탐구하는 사람 (Inquirer)
  {
    id: 'inq-001',
    title: '궁금한 거 3가지 질문하기',
    description: '오늘 하루 동안 "왜?"라고 3번 이상 질문해보세요',
    targetIBProfile: 'Inquirer',
    difficulty: 1,
    estimatedTime: '하루 종일',
    badge: { name: '호기심 탐험가', icon: '🔍', tier: 'bronze' },
    ageRange: { min: 6, max: 12 },
    category: 'analytical'
  },
  {
    id: 'inq-002',
    title: '실험 관찰 일지 작성',
    description: '간단한 실험을 하고 결과를 그림과 글로 기록해보세요',
    targetIBProfile: 'Inquirer',
    difficulty: 2,
    estimatedTime: '1시간',
    badge: { name: '꼬마 과학자', icon: '🔬', tier: 'silver' },
    ageRange: { min: 7, max: 12 },
    category: 'analytical'
  },

  // 도전하는 사람 (Risk-taker)
  {
    id: 'risk-001',
    title: '처음 해보는 것 도전하기',
    description: '한 번도 해보지 않은 새로운 활동에 도전해보세요',
    targetIBProfile: 'Risk-taker',
    difficulty: 2,
    estimatedTime: '자유',
    badge: { name: '용감한 도전자', icon: '🚀', tier: 'bronze' },
    ageRange: { min: 6, max: 12 },
    category: 'leadership'
  },

  // 배려하는 사람 (Caring)
  {
    id: 'care-001',
    title: '친구에게 친절하게 대하기',
    description: '오늘 하루 친구 한 명에게 도움을 주거나 친절하게 대해보세요',
    targetIBProfile: 'Caring',
    difficulty: 1,
    estimatedTime: '하루 종일',
    badge: { name: '따뜻한 마음', icon: '❤️', tier: 'bronze' },
    ageRange: { min: 6, max: 12 },
    category: 'caring'
  },

  // 성찰하는 사람 (Reflective)
  {
    id: 'ref-001',
    title: '오늘의 일기 쓰기',
    description: '오늘 있었던 일과 내 기분을 일기로 써보세요',
    targetIBProfile: 'Reflective',
    difficulty: 1,
    estimatedTime: '15분',
    badge: { name: '생각 기록가', icon: '📝', tier: 'bronze' },
    ageRange: { min: 7, max: 12 },
    category: 'analytical'
  }
];
```

#### 4.1.5 상세 기능

| 기능 | 설명 | 우선순위 |
|------|------|---------|
| 미션 추천 | IB 학습자상 기반 맞춤 미션 | P0 |
| 미션 시작/완료 | 상태 관리 | P0 |
| 배지 획득 | 완료 시 자동 배지 부여 | P0 |
| 배지 컬렉션 | 획득한 배지 모아보기 | P1 |
| 난이도별 필터 | 쉬움/보통/어려움 필터 | P1 |
| 부모 확인 | 부모가 완료 확인 | P2 |
| 소감 작성 | 완료 후 한 줄 소감 | P1 |

---

### 4.2 대화 카드 시스템 (`/conversation`)

#### 4.2.1 기능 설명
상황별 부모-아이 대화 주제를 카드 형태로 제공

#### 4.2.2 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  💬 오늘의 대화 카드                                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [상황 선택]                                             │
│  🍽️ 식사시간  🛏️ 취침전  🚗 이동중  🎮 놀이시간          │
│                                                         │
│  ────────────────────────────────────────────────       │
│                                                         │
│  [오늘의 추천 질문] 🔍 탐구하는 사람 강화                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │                                                 │   │
│  │  "오늘 학교에서 제일 궁금했던 건 뭐야?"           │   │
│  │                                                 │   │
│  │  💡 대화 팁:                                     │   │
│  │  아이가 답하면 "그래서 어떻게 됐어?"라고          │   │
│  │  이어서 물어보세요                               │   │
│  │                                                 │   │
│  │  [🔄 다른 질문 보기]  [✅ 오늘 사용했어요]         │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [IB 학습자상별 질문 모음]                               │
│  🔍 탐구하는  💭 생각하는  ❤️ 배려하는  🚀 도전하는      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 4.2.3 대화 카드 데이터 구조

```typescript
interface ConversationCard {
  id: string;
  question: string;
  situation: 'meal' | 'bedtime' | 'travel' | 'play' | 'anytime';
  targetIBProfile: IBProfile;
  followUpTips: string[];
  ageRange: {
    min: number;
    max: number;
  };
  category: 'question' | 'activity' | 'reflection';
}

interface ConversationHistory {
  id: string;
  childId: string;
  cardId: string;
  usedAt: string;
  notes?: string;               // 부모 메모 (선택)
}
```

#### 4.2.4 상황별 대화 카드 예시

```typescript
const CONVERSATION_CARDS: ConversationCard[] = [
  // 식사 시간
  {
    id: 'meal-inq-001',
    question: '오늘 학교에서 제일 궁금했던 건 뭐야?',
    situation: 'meal',
    targetIBProfile: 'Inquirer',
    followUpTips: [
      '그래서 어떻게 됐어?',
      '선생님한테 물어봤어?',
      '우리 같이 찾아볼까?'
    ],
    ageRange: { min: 6, max: 12 },
    category: 'question'
  },

  // 취침 전
  {
    id: 'bed-ref-001',
    question: '오늘 하루 중 제일 뿌듯했던 순간은?',
    situation: 'bedtime',
    targetIBProfile: 'Reflective',
    followUpTips: [
      '왜 그게 뿌듯했어?',
      '내일도 그런 기분 느끼고 싶으면 뭘 하면 좋을까?'
    ],
    ageRange: { min: 6, max: 12 },
    category: 'reflection'
  },

  // 이동 중
  {
    id: 'travel-think-001',
    question: '저기 있는 건물은 왜 저렇게 생겼을까?',
    situation: 'travel',
    targetIBProfile: 'Thinker',
    followUpTips: [
      '다르게 만들면 어떨 것 같아?',
      '네가 만든다면 어떻게 만들겠어?'
    ],
    ageRange: { min: 6, max: 12 },
    category: 'question'
  },

  // 놀이 시간
  {
    id: 'play-risk-001',
    question: '새로운 놀이 방법을 만들어볼까?',
    situation: 'play',
    targetIBProfile: 'Risk-taker',
    followUpTips: [
      '규칙을 바꿔볼까?',
      '다른 도구를 써보면 어떨까?'
    ],
    ageRange: { min: 6, max: 12 },
    category: 'activity'
  }
];
```

#### 4.2.5 상세 기능

| 기능 | 설명 | 우선순위 |
|------|------|---------|
| 오늘의 질문 | 매일 IB 기반 추천 질문 | P0 |
| 상황별 필터 | 식사/취침/이동/놀이 | P0 |
| 새로고침 | 다른 질문 보기 | P0 |
| 사용 기록 | 오늘 사용한 질문 체크 | P1 |
| 대화 팁 | 후속 질문 가이드 | P0 |
| 즐겨찾기 | 좋았던 질문 저장 | P2 |

---

### 4.3 성장 다이어리 (`/diary`)

#### 4.3.1 기능 설명
활동 사진과 한 줄 소감을 기록하는 성장 일지

#### 4.3.2 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  📔 성장 다이어리                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [+ 새 기록 추가]                                        │
│                                                         │
│  ────────────────────────────────────────────────       │
│                                                         │
│  [타임라인 뷰]                                           │
│                                                         │
│  📅 2024.12.22                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [사진]  도서관에서 공룡 책 읽기 📚                 │   │
│  │         "공룡 뼈가 어떻게 화석이 되는지           │   │
│  │          알게 됐어요!"                            │   │
│  │                                                 │   │
│  │ 🔍 탐구하는 사람  |  🏅 호기심 탐험가 획득         │   │
│  │                                                 │   │
│  │ 👪 부모 메모: 스스로 책을 골라서 2시간 동안        │   │
│  │    집중해서 읽었어요. 집에 와서도 공룡 이야기만!   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  📅 2024.12.20                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [사진]  친구 생일파티에서 🎂                      │   │
│  │         "케이크 만들기 도와줬어요"                │   │
│  │                                                 │   │
│  │ ❤️ 배려하는 사람                                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 4.3.3 다이어리 데이터 구조

```typescript
interface DiaryEntry {
  id: string;
  childId: string;
  date: string;

  // 활동 기록
  activity: {
    title: string;
    description: string;
    photos: string[];           // base64 또는 URL (최대 3장)
  };

  // 아이 소감
  childReflection: string;

  // 부모 메모 (선택)
  parentNote?: string;

  // 연결 정보
  relatedMissionId?: string;    // 연관된 미션
  relatedGoalId?: string;       // 연관된 목표
  ibProfile?: IBProfile;        // 관련 IB 학습자상
  badge?: Badge;                // 획득한 배지

  // 메타데이터
  createdAt: string;
  updatedAt: string;
}
```

#### 4.3.4 상세 기능

| 기능 | 설명 | 우선순위 |
|------|------|---------|
| 기록 추가 | 사진 + 소감 입력 | P0 |
| 타임라인 뷰 | 날짜순 기록 보기 | P0 |
| 부모 메모 | 부모가 추가 메모 작성 | P1 |
| 미션 연결 | 완료 미션과 자동 연결 | P1 |
| IB 태그 | 관련 IB 학습자상 태그 | P1 |
| 캘린더 뷰 | 월간 활동 캘린더 | P2 |
| 검색/필터 | IB별, 기간별 필터 | P2 |

---

## 5. Phase 3: 포트폴리오 & 리포트

### 5.1 성장 포트폴리오 (`/portfolio`)

#### 5.1.1 기능 설명
아이의 성장 여정을 종합한 포트폴리오 문서 생성

#### 5.1.2 포트폴리오 구성

```
┌─────────────────────────────────────────────────────────┐
│  📁 민준이의 성장 포트폴리오                              │
│                                               [PDF 저장] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [1. 프로필 요약]                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 이름: 민준  |  나이: 10세  |  기간: 2024.09-12   │   │
│  │                                                 │   │
│  │ 주요 IB 학습자상:                                │   │
│  │ 🔍 탐구하는 사람  💭 생각하는 사람                │   │
│  │                                                 │   │
│  │ 주요 직업군: 창의·예술, 분석·연구                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [2. 성장 그래프]                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [시간에 따른 직업군 점수 변화 그래프]             │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [3. 주요 활동 하이라이트]                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 🏆 완료한 미션: 12개                              │   │
│  │ 🏅 획득한 배지: 8개                               │   │
│  │                                                 │   │
│  │ 대표 활동:                                       │   │
│  │ • 과학 실험 관찰 일지 프로젝트                    │   │
│  │ • 도서관 탐구 활동                               │   │
│  │ • 창의적 글쓰기                                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [4. 사진 갤러리]                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [사진1] [사진2] [사진3] [사진4]                   │   │
│  │ [사진5] [사진6] [사진7] [사진8]                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [5. AI 성장 분석]                                       │
│  ┌─────────────────────────────────────────────────┐   │
│  │ "민준이는 지난 3개월간 탐구력과 창의적 사고력이   │   │
│  │  크게 성장했습니다. 특히 과학 분야에 대한 호기심  │   │
│  │  이 두드러지며, 질문하고 탐구하는 습관이          │   │
│  │  자리잡고 있습니다..."                           │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 5.1.3 상세 기능

| 기능 | 설명 | 우선순위 |
|------|------|---------|
| 자동 생성 | 기록 데이터 기반 자동 구성 | P0 |
| PDF 내보내기 | A4 형식 PDF 다운로드 | P0 |
| 사진 선택 | 포함할 사진 선택 | P1 |
| 기간 설정 | 포트폴리오 기간 범위 | P1 |
| AI 분석 포함 | 성장 분석 코멘트 | P1 |
| 커스텀 편집 | 내용 직접 수정 | P2 |

---

### 5.2 AI 성장 분석 리포트 (`/report`)

#### 5.2.1 기능 설명
분기별 AI가 생성하는 종합 성장 분석 리포트

#### 5.2.2 리포트 구성

```typescript
interface GrowthReport {
  id: string;
  childId: string;
  period: {
    start: string;
    end: string;
    type: 'monthly' | 'quarterly' | 'yearly';
  };

  // 검사 결과 비교
  assessmentComparison: {
    previous: AssessmentResult;
    current: AssessmentResult;
    changes: {
      category: CareerCategory;
      previousScore: number;
      currentScore: number;
      change: number;
      changePercent: number;
    }[];
  };

  // 활동 통계
  activityStats: {
    totalMissionsCompleted: number;
    totalBadgesEarned: number;
    totalDiaryEntries: number;
    totalCheckIns: number;
    mostActiveIBProfile: IBProfile;
    consistencyScore: number;        // 꾸준함 점수 (0-100)
  };

  // AI 분석
  aiAnalysis: {
    summary: string;                  // 전체 요약
    strengths: string[];              // 강점
    growthAreas: string[];            // 성장한 영역
    recommendations: string[];        // 다음 분기 추천
    parentTips: string[];             // 부모를 위한 팁
  };

  // 메타데이터
  generatedAt: string;
}
```

#### 5.2.3 AI 리포트 생성 프롬프트

```typescript
const reportGenerationPrompt = `
아이 성장 데이터를 분석하여 종합 리포트를 작성해주세요.

[아이 정보]
- 이름: ${nickname}
- 나이: ${age}세
- 분석 기간: ${period.start} ~ ${period.end}

[검사 결과 변화]
이전 검사 (${previousDate}):
- 상위 직업군: ${previousTopCategories}
- IB 학습자상: ${previousIBProfiles}

현재 검사 (${currentDate}):
- 상위 직업군: ${currentTopCategories}
- IB 학습자상: ${currentIBProfiles}

점수 변화:
${scoreChanges.map(c => `- ${c.category}: ${c.previousScore} → ${c.currentScore} (${c.change > 0 ? '+' : ''}${c.change})`).join('\n')}

[활동 기록]
- 완료한 미션: ${totalMissions}개
- 획득한 배지: ${totalBadges}개
- 다이어리 기록: ${totalEntries}개
- 주간 체크인: ${totalCheckIns}회
- 가장 활발한 IB 학습자상: ${mostActiveIBProfile}

[부모 관찰 메모 요약]
${parentObservations}

위 데이터를 바탕으로 다음 형식으로 분석 리포트를 작성해주세요:

1. 전체 요약 (3-4문장)
2. 강점 (3가지)
3. 성장한 영역 (구체적인 변화 포함)
4. 다음 분기 추천 활동 (3가지)
5. 부모를 위한 팁 (2가지)

따뜻하고 격려하는 톤으로, 아이와 부모 모두 읽을 수 있게 작성해주세요.
`;
```

---

## 6. 데이터 저장 전략

> 📚 **상세 기술 문서**: [TECHNICAL_INFRASTRUCTURE.md](./TECHNICAL_INFRASTRUCTURE.md) 참조

### 6.1 저장소 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                      클라이언트                          │
│  Next.js 15 + Supabase SSR Client                      │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      Supabase                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  PostgreSQL  │  │    Auth      │  │   Storage    │  │
│  │  (Database)  │  │  (인증)      │  │  (이미지)    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 6.2 데이터베이스 테이블 구조

| 테이블명 | 설명 | 주요 컬럼 |
|---------|------|----------|
| `profiles` | 사용자 프로필 | id, email, display_name |
| `children` | 아이 정보 | nickname, age, activities, hobbies |
| `assessments` | 검사 결과 | scores, ib_profiles, ai_insights |
| `goals` | 성장 목표 | title, target_ib_profile, progress |
| `mission_templates` | 미션 템플릿 (시스템) | title, difficulty, badge_tier |
| `mission_progress` | 미션 진행 | status, completed_at, reflection |
| `badges` | 획득 배지 | name, icon, tier, earned_at |
| `check_ins` | 주간 체크인 | child_eval, parent_obs |
| `diary_entries` | 성장 다이어리 | activity, photos, reflection |
| `conversation_cards` | 대화 카드 (시스템) | question, situation, tips |
| `conversation_history` | 대화 사용 기록 | card_id, used_at, notes |
| `reports` | AI 성장 리포트 | ai_analysis, generated_at |

### 6.3 보안 (Row Level Security)

```sql
-- 모든 테이블에 RLS 적용
-- 사용자는 자신의 아이 데이터만 접근 가능
CREATE POLICY "Users can CRUD own children data"
  ON public.children FOR ALL
  USING (auth.uid() = user_id);

-- 아이 관련 데이터는 부모 소유 아이만 접근
CREATE POLICY "Users can CRUD own children assessments"
  ON public.assessments FOR ALL
  USING (
    child_id IN (
      SELECT id FROM public.children WHERE user_id = auth.uid()
    )
  );
```

### 6.4 로컬 개발 호환

```typescript
// 로컬 개발 시 localStorage 폴백 지원
const STORAGE_KEYS = {
  RESULTS: 'child-future-results',
  GOALS: 'child-future-goals',
  MISSIONS: 'child-future-missions',
  MISSION_PROGRESS: 'child-future-mission-progress',
  BADGES: 'child-future-badges',
  CHECK_INS: 'child-future-checkins',
  DIARY: 'child-future-diary',
  CONVERSATION_HISTORY: 'child-future-conversations',
  REPORTS: 'child-future-reports',
  CHILD_PROFILES: 'child-future-profiles',
  SETTINGS: 'child-future-settings'
};
```

### 6.5 마이그레이션 전략

```
기존 localStorage 데이터 → Supabase 마이그레이션
├── 1. 사용자 로그인 시 localStorage 확인
├── 2. 미마이그레이션 데이터 발견 시 마이그레이션 안내
├── 3. 닉네임별 아이 프로필 자동 생성
├── 4. 검사 결과 → assessments 테이블 저장
└── 5. 마이그레이션 완료 표시
```

---

## 7. UI/UX 가이드라인

### 7.1 네비게이션 구조

```
[메인]
├── 검사하기 (/basic-info → /assessment → /results)
├── 성장관리
│   ├── 히스토리 (/history)
│   ├── 목표 (/goals)
│   └── 체크인 (/check-in)
├── 활동
│   ├── 미션 (/missions)
│   ├── 대화카드 (/conversation)
│   └── 다이어리 (/diary)
├── 리포트
│   ├── 포트폴리오 (/portfolio)
│   └── 성장분석 (/report)
└── 설정 (/settings)
```

### 7.2 사용자 모드

```typescript
type UserMode = 'child' | 'parent' | 'together';

// 모드별 UI 차이
const UI_DIFFERENCES = {
  child: {
    fontSize: 'large',
    emoji: 'heavy',
    language: 'simple',
    features: ['missions', 'diary', 'badges']
  },
  parent: {
    fontSize: 'normal',
    emoji: 'moderate',
    language: 'detailed',
    features: ['all']
  },
  together: {
    fontSize: 'large',
    emoji: 'moderate',
    language: 'simple',
    features: ['check-in', 'conversation', 'diary']
  }
};
```

### 7.3 반응형 디자인

```
모바일: 세로 스크롤 중심, 큰 터치 타겟
태블릿: 2컬럼 레이아웃, 부모-아이 동시 사용 고려
데스크톱: 대시보드 형태, 한 눈에 현황 파악
```

---

## 8. 개발 우선순위 로드맵

### Phase 1 (MVP) - 4주

| 주차 | 기능 | 우선순위 |
|------|------|---------|
| 1주 | 결과 히스토리 페이지 | P0 |
| 2주 | 성장 목표 설정 | P0 |
| 3주 | 주간 체크인 | P0 |
| 4주 | 통합 테스트 & 버그 수정 | P0 |

### Phase 2 - 4주

| 주차 | 기능 | 우선순위 |
|------|------|---------|
| 5주 | 활동 미션 시스템 | P0 |
| 6주 | 대화 카드 시스템 | P0 |
| 7주 | 성장 다이어리 | P1 |
| 8주 | 배지 시스템 완성 | P1 |

### Phase 3 - 3주

| 주차 | 기능 | 우선순위 |
|------|------|---------|
| 9주 | 성장 포트폴리오 | P1 |
| 10주 | AI 성장 분석 리포트 | P1 |
| 11주 | PDF 내보내기 & 공유 | P1 |

---

## 9. 성공 지표 (KPI)

### 9.1 사용자 참여 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 주간 체크인 완료율 | 60% 이상 | 체크인 수 / 활성 사용자 |
| 미션 완료율 | 40% 이상 | 완료 미션 / 시작 미션 |
| 월간 재방문율 | 70% 이상 | 재방문 사용자 / 전체 사용자 |
| 평균 세션 시간 | 5분 이상 | 세션 시작-종료 시간 |

### 9.2 성장 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 재검사율 | 30% 이상 | 2회 이상 검사 사용자 비율 |
| 목표 달성률 | 50% 이상 | 완료 목표 / 설정 목표 |
| 포트폴리오 생성률 | 20% 이상 | 포트폴리오 생성 사용자 비율 |

---

## 10. 부록: 타입 정의 전체

```typescript
// types/growth.ts

// === 목표 관련 ===
export interface GrowthGoal {
  id: string;
  childId: string;
  title: string;
  description: string;
  targetIBProfile: IBProfile;
  targetCategory: CareerCategory;
  period: {
    start: string;
    end: string;
  };
  recommendedActivities: string[];
  status: 'active' | 'completed' | 'abandoned';
  progress: number;
  createdAt: string;
  updatedAt: string;
}

// === 미션 관련 ===
export interface Mission {
  id: string;
  title: string;
  description: string;
  targetIBProfile: IBProfile;
  difficulty: 1 | 2 | 3;
  estimatedTime: string;
  badge: BadgeDefinition;
  ageRange: { min: number; max: number };
  category: CareerCategory;
}

export interface MissionProgress {
  id: string;
  childId: string;
  missionId: string;
  status: 'in_progress' | 'completed' | 'skipped';
  startedAt: string;
  completedAt?: string;
  reflection?: string;
  parentVerified: boolean;
}

export interface BadgeDefinition {
  name: string;
  icon: string;
  tier: 'bronze' | 'silver' | 'gold';
}

export interface Badge extends BadgeDefinition {
  id: string;
  childId: string;
  missionId: string;
  earnedAt: string;
}

// === 체크인 관련 ===
export interface WeeklyCheckIn {
  id: string;
  childId: string;
  weekStart: string;
  weekEnd: string;
  childEvaluation: {
    activityLevel: 'high' | 'medium' | 'low';
    favoriteActivity: string;
    weeklyMood: '😄' | '😊' | '🙂' | '😐' | '😢';
    freeNote?: string;
  };
  parentObservation: {
    noticeableChanges: string;
    completedActivities: string[];
    specialEpisode?: string;
    photos?: string[];
  };
  createdAt: string;
  updatedAt: string;
}

// === 다이어리 관련 ===
export interface DiaryEntry {
  id: string;
  childId: string;
  date: string;
  activity: {
    title: string;
    description: string;
    photos: string[];
  };
  childReflection: string;
  parentNote?: string;
  relatedMissionId?: string;
  relatedGoalId?: string;
  ibProfile?: IBProfile;
  badge?: Badge;
  createdAt: string;
  updatedAt: string;
}

// === 대화 카드 관련 ===
export interface ConversationCard {
  id: string;
  question: string;
  situation: 'meal' | 'bedtime' | 'travel' | 'play' | 'anytime';
  targetIBProfile: IBProfile;
  followUpTips: string[];
  ageRange: { min: number; max: number };
  category: 'question' | 'activity' | 'reflection';
}

export interface ConversationHistory {
  id: string;
  childId: string;
  cardId: string;
  usedAt: string;
  notes?: string;
}

// === 리포트 관련 ===
export interface GrowthReport {
  id: string;
  childId: string;
  period: {
    start: string;
    end: string;
    type: 'monthly' | 'quarterly' | 'yearly';
  };
  assessmentComparison: {
    previous: AssessmentResult;
    current: AssessmentResult;
    changes: ScoreChange[];
  };
  activityStats: ActivityStats;
  aiAnalysis: AIAnalysis;
  generatedAt: string;
}

export interface ScoreChange {
  category: CareerCategory;
  previousScore: number;
  currentScore: number;
  change: number;
  changePercent: number;
}

export interface ActivityStats {
  totalMissionsCompleted: number;
  totalBadgesEarned: number;
  totalDiaryEntries: number;
  totalCheckIns: number;
  mostActiveIBProfile: IBProfile;
  consistencyScore: number;
}

export interface AIAnalysis {
  summary: string;
  strengths: string[];
  growthAreas: string[];
  recommendations: string[];
  parentTips: string[];
}

// === 앱 설정 ===
export interface AppSettings {
  userMode: 'child' | 'parent' | 'together';
  reminderEnabled: boolean;
  reminderDay: 'sunday' | 'saturday';
  reminderTime: string;
  theme: 'light' | 'dark' | 'auto';
}

// === 전체 앱 데이터 ===
export interface AppData {
  version: string;
  results: AssessmentResult[];
  goals: GrowthGoal[];
  missionProgress: MissionProgress[];
  badges: Badge[];
  checkIns: WeeklyCheckIn[];
  diary: DiaryEntry[];
  conversationHistory: ConversationHistory[];
  reports: GrowthReport[];
  settings: AppSettings;
  lastUpdated: string;
}
```

---

## 문서 끝

이 문서는 프로젝트 확장의 기준이 되는 기능 정의서입니다.
개발 진행에 따라 상세 내용이 업데이트될 수 있습니다.
